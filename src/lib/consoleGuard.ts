/**
 * Console Guard Utility - RELAXED
 * Security features have been softened to prevent affecting normal users.
 */

export const initConsoleGuard = () => {
  // Features removed to prevent false positives and improve UX
  return;
};
